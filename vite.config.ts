import { defineConfig } from "vite";

export default defineConfig({
  // Change to your repo name for GitHub Pages: '/repo-name/'
  // Use '/' if deploying to a custom domain or username.github.io
  base: "/Engagement/",
});
